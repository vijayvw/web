apiVersion: v1
kind: Service
metadata:
  name: login-page-service

spec:
  selector:
    app: login-page

  type: NodePort

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
